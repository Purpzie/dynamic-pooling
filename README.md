# dynamic-pooling

A lock-free, thread-safe object pool for Rust.

*[`dynamic-pool`](https://crates.io/crates/dynamic-pool) doesn't seem to be actively maintained, so
this crate is an unofficial replacement.*
